# .agent-rules.md — Agent 가드레일 (50% 룰 + 패턴 통일)

> 이 파일은 `CLAUDE.md`의 **보충 자산**입니다. 50% 룰 적발(같은 일을 하는 경쟁 패턴)을 명문화해
> Agent의 variance를 줄이고, "어떤 패턴을 쓸지"를 결정적으로 만듭니다.
> CLAUDE.md가 *무엇이 금지인지*를 말한다면, 이 파일은 *대신 무엇을 써야 하는지*(✅ vs 🚫)를 한 줄로 못박습니다.

마지막 업데이트: 2026-06-04

> **이 문서는 살아 있는 문서입니다.** 이 앱은 의도적으로 MVP(단일 `index.html`)라 지금 적발할 경쟁 패턴이 많지 않습니다.
> 그래서 각 룰에 **상태 표시**를 답니다:
> - 🟢 **단일 패턴 유지 중** — 아직 경쟁 패턴이 코드에 없음. 깨지면(두 번째 방법이 등장하면) 그때 여기 🟡로 적발.
> - 🟡 **경쟁 패턴 적발됨** — 잘못된 쪽을 명시하고 마이그레이션 방향을 적음.
> - 🔒 **가드레일 테스트가 강제** — `tests/guardrails.test.js`가 커밋 직후 자동 검사(어기면 빨간불).
> - 👁 **판단 의존** — 자동 검사 없음. Agent/사람의 규율에만 의존 → **진짜 50% 위험 지점**.
>
> 새 경쟁 패턴을 발견하면 룰을 추가하거나 상태를 갱신하고, 맨 아래 **변경 이력**에 한 줄 남깁니다.

---

## 1. 패턴 통일 (50% 룰 적발)

> 같은 일을 하는 두 가지 이상의 방법이 코드베이스에 공존하면, agent가 ~50% 확률로 잘못된 쪽을 고릅니다.
> 아래는 이 프로젝트에서 "한 가지 일 = 한 가지 blessed 경로"를 고정한 목록입니다. 모듈 책임 전체는 `@.claude/docs/architecture.md` 4절 참조.

### Rule 1 — localStorage 입출력  🔒 가드레일 강제 · 🟢 단일 패턴
- ✅ 사용: `Store.load / save / remove / getStr / setStr / eachKey / newId`
- 🚫 금지: 코드 어디서든 `localStorage.getItem/setItem/...` **직접 호출** (`Store` 내부 제외)
- 위치: `index.html` `<script>` 상단 `const Store = { ... }`
- 근거: ADR-002. 키 규칙·마이그레이션을 한 창구에서 관리. `tests/guardrails.test.js`가 Store 밖 `localStorage.` 호출을 잡는다.

### Rule 2 — 날짜 키 생성  🔒 가드레일 강제 · 🟢 단일 패턴
- ✅ 사용: `DateUtil.toKey(date)` (로컬 자정 `YYYY-MM-DD`), 경계 비교는 `DateUtil.startOfDay()`
- 🚫 금지: `date.toISOString()`를 **날짜 키 용도**로 사용, 또는 `getFullYear()+...`를 `DateUtil` 밖에서 손으로 조립
- 위치: `const DateUtil = { ... }`
- 근거: ADR-004. `toISOString()`은 UTC라 자정 근처에 하루가 어긋난다. 가드레일이 `.toISOString(` 호출 자체를 금지한다.

### Rule 3 — 상태 변경 후 다시 그리기 (단일 렌더 경로)  👁 판단 의존 · 🟢 단일 패턴
- ✅ 사용: 데이터를 바꾼 뒤 **`renderAll()` 또는 `refreshDerived()` 중 하나만** 호출
  - `renderAll()` — 날짜 전환·초기 로드처럼 헤더/회고 입력칸까지 전부 새로 그릴 때
  - `refreshDerived()` — 카드 추가·이동·삭제·회고 저장처럼 데이터 의존 뷰(Board/Calendar/Stats)만 갱신할 때
- 🚫 금지: 모듈 `render()` 밖에서 `innerHTML = ` / `appendChild` 등으로 **DOM을 직접 패치**해 화면을 부분 갱신하는 것
- 근거: architecture.md 3절. `recorded` 파생 캐시를 렌더당 1회만 계산하는 계약이 깨지면 통계·달력이 어긋난다.
- ⚠️ 이 룰은 자동 검사가 없다 → 새 기능 추가 시 가장 빠지기 쉬운 함정. "DOM을 직접 만지고 싶다"는 충동이 들면 그 로직을 해당 모듈의 `render()`로 옮기고 렌더 경로를 호출하라.

### Rule 4 — 날짜 이동  👁 판단 의존 · 🟢 단일 패턴
- ✅ 사용: 보고 있는 날짜를 바꿀 땐 **`navigateTo(date)`** (내부에서 `Journal.flush()` → `viewDate` 변경 → `loadDay()`)
- 🚫 금지: `App.state.viewDate`를 직접 대입한 뒤 `loadDay()`만 호출 (디바운스 중인 회고가 flush되지 않아 유실)
- 근거: architecture.md 3절. 미래 날짜 차단도 이 경로가 보장한다.

### Rule 5 — 사용자 알림  👁 판단 의존 · 🟢 단일 패턴
- ✅ 사용: 가벼운 안내·성공/실패 토스트는 **`Toast.show("...")`**. AI 결과/에러는 각 전용 `.ai-out` 박스.
- ✅ 예외(의도된 사용): **파괴적 동작 확인**은 네이티브 `confirm()` OK — 카드 삭제(`Modal.del`), 백업 가져오기 덮어쓰기(`Backup.import`)에서 실제로 쓴다.
- 🚫 금지: 단순 알림에 `alert()`. 사용자에게 보여줄 내용을 `console.log`로 내보내지 말 것(콘솔은 개발자용). 임시 디버깅 `console.*` 잔재는 커밋 전 제거 — **구조화된 개발 로그는 §3의 `logger`를 쓴다**.
- 근거: CLAUDE.md 4절 "알림/에러". UI 텍스트는 모두 한국어, 친절한 톤.

### Rule 6 — id 생성  👁 판단 의존 · 🟢 단일 패턴
- ✅ 사용: 카드 등 새 엔티티 id는 **`Store.newId()`**
- 🚫 금지: `Date.now()` / `Math.random()` / `crypto.randomUUID()`를 호출부에서 직접 조합
- 근거: 생성 규칙을 한 곳에 고정해 충돌·포맷 분기를 막는다.

### Rule 7 — Claude API 호출  👁 판단 의존 · 🟢 단일 패턴
- ✅ 사용: 모든 LLM 호출은 **`AI.call(system, userText, maxTokens)`**, UI 래핑은 **`AI.run(btn, outEl, fn)`**(로딩 표시·키 누락 유도·친절한 한국어 에러·`file://` 차단 안내 포함). 응답 JSON은 `AI.parseJson()`으로 느슨하게 파싱.
- 🚫 금지: `api.anthropic.com`으로 **원시 `fetch()`를 새로 작성**, 또는 키를 `AI.key()` 밖에서 직접 읽기
- 위치: `const AI = { ... }`
- 근거: ADR-003. 헤더(`anthropic-dangerous-direct-browser-access`)·키 출처·에러 UX를 한 곳에 모은다. 4번째 AI 기능을 추가하더라도 이 두 함수를 거친다.

### Rule 8 — 색상  🟡 부분 적발 · 👁 판단 의존
- ✅ 사용(테마 표면색): 배경·보더·텍스트·호버·칩·강조 톤 등 **다크모드에서 바뀌어야 하는 색**은 반드시 `:root` CSS 변수로 정의하고 `:root[data-theme="dark"]` 팔레트에도 **같이** 추가한다.
- 🟡 알려진 예외(현재 코드에 공존): 일부 **시멘틱 상태색은 하드코딩**되어 있다 — 태그 배지(work/study), `overdue` 빨강, "저장됨" 초록(`#22a06b`), 에러 빨강(`#e5484d`), 달력 일/토(빨강/파랑), `modal-warn` 주황, `modal-del` 빨강. 이들은 의미가 고정된 강조라 변수화하지 않았다(단, `modal-warn`만 다크 오버라이드 있음).
- 🚫 금지: **새로** 추가하는 테마 표면색을 변수 없이 bare hex로 박는 것 / 변수를 한쪽 팔레트(라이트 or 다크)에만 넣는 것
- 권장: 새 시멘틱 색도 가능하면 변수로 올려 위 예외 목록을 늘리지 말 것.

---

### 1-b. 아직 경쟁 패턴이 없는 영역 (감시 목록)

> 지금은 단일 패턴이라 적발할 게 없지만, **두 번째 방법이 등장하는 순간** 여기서 1절의 정식 룰로 승격한다.

- **네트워크 호출** — 현재 `AI.call`의 `fetch` 한 곳뿐. 다른 외부 호출이 생기면 공용 래퍼로 통일할지 결정하고 Rule로 승격. (외부 `<script src>`/CDN 도입은 ADR-001 위반이라 가드레일이 차단)
- **모듈 패턴** — 전부 전역 `const X = { ... }`. 클래스/팩토리/즉시실행 등 다른 형태가 섞이면 여기 적발.
- **상태 보관** — 단일 진실 원천은 `App.state`. 별도 전역 변수에 상태를 흩뿌리는 코드가 보이면 적발.
- **시간 포맷** — `HH:MM`은 `DateUtil.nowHHMM()` 한 곳. 두 번째 시간 포맷 헬퍼가 생기면 통일.
- *(빈 자리 — 새 경쟁 패턴을 발견하면 여기에 적고, 굳어지면 1절로 올린다.)*

---

## 2. 에이전트 위임 가드레일 (Pillar IV)

> 이 앱은 **단일 파일**이라 대규모 fan-out 탐색 수요는 작다. 위임은 "큰 출력 흡수" 또는 "독립 판단"이 필요할 때만.

### Sub-agent로 위임 OK
- `index.html` 안에서 특정 모듈/심볼 위치 찾기, 문서 간 교차 확인 — 큰 출력 흡수(Explore).
- staged diff에 대한 **독립 코드 리뷰** — 메인 컨텍스트와 분리된 객관적 평가(커밋 전 훅 ADR-006과는 별개 보완).

### Sub-agent로 위임 NO (메인 컨텍스트에서 사람이 검토)
- **절대 규칙에 닿는 변경** — 단일 파일 유지, `Store`/localStorage, 날짜 키, API 키 취급. 위반 시 영향이 크다.
- **의존성·빌드 도구 추가 시도** — ADR-001 위반이자 가드레일·CLAUDE.md 위반. 애초에 하지 않는다.
- **훅·가드레일 수정** (`.claude/hooks/*`, `tests/guardrails.test.js`, `.claude/settings.json`) — 안전망 자체를 바꾸는 일이라 메인에서 신중히.

---

## 3. 구조화 로그 / 관측 (Pillar IV — self-correction 입력)  🟡 도입됨 · 👁 판단 의존 · 🟢 단일 패턴

> ✅ **구조화 로그 도입됨.** `index.html`의 `const logger = { ... }`가 JSON line(한 줄 = 한 이벤트)을 **콘솔에** 출력한다. 의존성 0.
> (이 섹션은 한때 "현재 해당 없음"이었으나 `logger` 도입으로 실제 룰로 승격됨 — §5 변경 이력 참조.)

### Rule — 개발용 구조화 로그
- ✅ 사용: 개발/디버깅 로그는 **`logger.info({...})` / `logger.error({...})`**. 한 이벤트 = JSON 한 줄.
  - 필수 필드: **`ts`, `module`, `level`, `event`**. 선택: 도메인 컨텍스트(예: `input`).
  - **`ts`는 `Date.now()`(epoch millis)** 를 쓴다 — `toISOString()`은 절대 규칙(ADR-004)으로 금지(Rule 2와 동일).
- 🚫 금지: 구조화 이벤트를 원시 `console.log("문자열")`로 흩뿌리기 / `logger` 밖에서 임의 포맷으로 로깅 / `ts`에 `toISOString()`.
- 🆚 역할 구분: `logger`(개발자·콘솔) ≠ `Toast`(사용자 알림) ≠ `.ai-out`(AI 결과). 섞지 않는다.
- 위치: `index.html` `<script>`의 `const logger`(Toast 다음, Theme 앞).

### 아직 없는 것 — 로그 파일·자동 자기검증  🟢
> 🟢 **로그 파일·자동 tail/jq 자기검증은 아직 없다 — 생기면 여기 적는다.**
> 브라우저 정적 앱이라 `logger`는 **콘솔 출력에 그친다**(`.logs/dev.log` 같은 파일 없음). 따라서 "로그를 tail/jq로 필터링해 자기수정"하는 흐름은 아직 적용되지 않는다.
> **언제 채우나:** 백엔드 프록시 도입(ADR-003 supersede)으로 서버 로그가 생기면, 그때 파일 출력·자동 자기검증 흐름을 여기에 정식화한다.

이와 별개로 현재 이 프로젝트의 **자동 자기수정 입력**은 두 훅이다(상세: architecture.md 8절):
- **커밋 후 가드레일** — `node --test` 실패 출력이 `PostToolUse` 훅으로 Claude 컨텍스트에 되돌아온다(ADR-005).
- **커밋 전 자동 리뷰** — staged diff가 `PreToolUse` 훅으로 리뷰 대상으로 되돌아온다(ADR-006).

---

## 4. 컨벤션 (Consistency over DRY — Pillar IV)

> Agent에게는 **반복이 섣부른 추상화보다 저렴**하다. 새 추상화를 만들기보다 **기존 모듈의 모양을 복사**한다.

- **새 모듈 추가**: 전역 `const X = { ... }` + 상단 `// ===== X: 한 줄 책임 =====` 배너. 이벤트는 `bindEvents()`에, DOM 참조는 `cacheElements()`의 `ids` 배열에 등록. 상태 변경 후엔 1절 Rule 3의 렌더 경로로만 다시 그린다.
- **새 데이터 키**: `area:YYYY-MM-DD` 컨벤션을 따르고, 스키마 주석을 `<script>` 상단 "데이터 모델" 블록에 추가. 접근은 `Store`로만(Rule 1).
- **새 CSS**: 기존 클래스 네이밍(`kb-*`, `cal-*`, `modal-*`, `stat-*`)을 따른다. 색은 Rule 8(변수 + 양쪽 팔레트).
- **UI 텍스트·toast·주석**: 모두 한국어, 따뜻하고 간결한 톤.
- **모듈 경계 주석(`// ===== 모듈 =====`)을 깨지 말 것** — 가드레일·탐색이 이 형태를 가정한다.
- **문서 동기화**: 구조/렌더 경로/데이터 모델을 바꾸면 같은 커밋에서 `architecture.md`, 큰 결정은 `decisions.md`(새 ADR), 절대 규칙/컨벤션은 `CLAUDE.md`와 **이 파일**을 갱신하고 상단 날짜를 바꾼다(CLAUDE.md 7절).

---

## 5. 변경 이력

- 2026-06-04: 템플릿 뼈대를 이 프로젝트(단일 `index.html` 바닐라 앱)에 맞게 **최초 최신화**.
  - 일반 룰(axios/errors/prisma)을 실제 모듈 룰로 교체: Store(localStorage)·DateUtil(날짜 키)·단일 렌더 경로·navigateTo·Toast·Store.newId·AI.call/run·CSS 변수.
  - 각 룰에 상태 태그(🔒 가드레일 / 👁 판단 의존 / 🟢 단일 패턴 / 🟡 적발) 도입.
  - 구조화 로그(3절)는 브라우저 앱이라 "현재 해당 없음" 선언으로 보류, 채울 조건 명시.
  - "감시 목록"(1-b) 신설 — 경쟁 패턴이 생기면 정식 룰로 승격하는 자리.
- 2026-06-04: `index.html`에 구조화 로그 `logger` 모듈이 도입되어 **§3을 "현재 해당 없음" → 실제 룰로 승격**(JSON line · `ts`=`Date.now()` · `logger.info/error`). Rule 5의 `console.log` 문구를 `logger`와 정합되게 조정. 로그 파일·자동 tail/jq 자기검증은 여전히 미도입으로 명시.
- *(다음 항목: 새 경쟁 패턴을 적발하거나 룰 상태를 바꾸면 여기에 한 줄.)*
